(compile-profile #t)
(compile-file "sequence-datatype.ss")
(load "sequence-datatype.so")
(let ([x (make-seq 1 2 3)])
  (assert (list? x))
  (do ([i 2 (sub1 i)])
    ((zero? i))
    ;; do some set!s to ensure a vector
    (seq-set! x i 0)))
(profile-dump-data "/tmp/foo.pd")
(profile-load-data "/tmp/foo.pd")
(compile-file "sequence-datatype.ss")
(load "sequence-datatype.so")
(assert (vector? (make-seq 1 2 3)))
